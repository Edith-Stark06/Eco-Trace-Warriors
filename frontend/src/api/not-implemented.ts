/**
 * Placeholder marker for API methods scaffolded in Sprint 9.1.
 *
 * Each API module exports fully typed method signatures whose bodies are not
 * implemented yet (this sprint is infrastructure only — no backend logic).
 * Calling one throws a clear, traceable error instead of failing silently.
 */
export function notImplemented(method: string): never {
  throw new Error(`[EcoTrace] API method not implemented yet: ${method}`);
}
