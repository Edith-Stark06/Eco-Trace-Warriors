/** Sidebar navigation placeholder. Role-based nav links arrive in later sprints. */
export function Sidebar() {
  return (
    <aside className="hidden w-56 shrink-0 border-r bg-background p-4 md:block">
      <nav aria-label="Primary" className="text-sm text-muted-foreground">
        {/* Placeholder: navigation links are added per role in later sprints. */}
        <p>Navigation</p>
      </nav>
    </aside>
  );
}
